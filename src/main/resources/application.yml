server:
  port: 8080

spring:
  application:
    name: spring-ai-local-llm

  ai:
    ollama:
      base-url: http://localhost:11434

      chat:
        options:
          model: llama3
          temperature: 0.7